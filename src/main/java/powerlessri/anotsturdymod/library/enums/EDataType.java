package powerlessri.anotsturdymod.library.enums;

public enum EDataType {

	UNKOWN(),

	BOOLEAN(), BYTE(), INT(), BYTE_ARRAY(), INT_ARRAY(), SHORT(), LONG(), FLOAT(), DOUBLE(), STRING(), NBT_TAG(),
	NBT_LIST();

}
